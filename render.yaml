services:
  - type: web
    name: smartroute-backend
    runtime: docker
    dockerfilePath: ./server/Dockerfile
    dockerContext: ./server
    plan: free
    envVars:
      - key: DATABASE_URL
        sync: false
    healthCheckPath: /health
